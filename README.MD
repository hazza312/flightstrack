# KLM Flight Offer Tracker
Experimenting with KLM flight offers API to build a simple report. Work in progress..

## Usage
```
docker-compose up
docker-compose run report [--nofetch] [--output=path/to/reportoutput.html]
```
